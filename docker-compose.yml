version: '3.8'
services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: testdb  # ← Changez pour correspondre à votre properties
      MYSQL_USER: user        # ← Changez pour correspondre à votre properties
      MYSQL_PASSWORD: userpass # ← Changez pour correspondre à votre properties
      MYSQL_ROOT_HOST: '%'
    command: 
      - --bind-address=0.0.0.0
      - --default-authentication-plugin=mysql_native_password
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  student-app:
    build: .
    container_name: student-management
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3306/testdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: user
      SPRING_DATASOURCE_PASSWORD: userpass
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SERVER_PORT: 8089
      SERVER_SERVLET_CONTEXT_PATH: /courses-api
    ports:
      - "8083:8089"
    depends_on:
      mysql-db:
        condition: service_healthy

volumes:
  mysql_data: